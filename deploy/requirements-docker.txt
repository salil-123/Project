# Serving-only deps for the LULC web app image. Trimmed from the repo's requirements.txt:
# no jupyter/ipykernel/pytest (dev), no geemap/contextily (unused), no xarray/zarr/geotessera
# (only the unexposed Tessera path — imported lazily, so its endpoints just error if ever called).
earthengine-api
geopandas
pyogrio            # geopandas IO engine (bundles GDAL) — reads uploaded GeoJSON/KML
shapely
pyproj
rasterio           # GeoTIFF export path
pandas
numpy
scikit-learn
joblib
jsonschema
fastapi
python-multipart   # FastAPI needs this for the file-upload (UploadFile) endpoints
uvicorn[standard]
python-dotenv
# xgboost is intentionally OUT of the image: its wheel drags in nvidia-nccl-cu12 (~300 MB of GPU
# libs unused on CPU), and sklearn RandomForest already covers the non-linear case. refine._has_xgb()
# degrades gracefully (the Tessera+XGBoost option just shows as unavailable). Re-add if truly needed.
