# Tessera Embedding Request — ready to file

Open a new issue in **ucam-eo/geotessera** (New issue → "Embedding Request") and fill the fields
below. Attach `sites.zip` (in this folder) when you reach the ROI step — a shapefile takes
precedence over the bbox, so the four coordinate boxes can be left blank.

> Placeholders to set before submitting: **organization** (I used IIT Delhi / CoRE Stack — change if
> needed) and the **years** you actually want. Everything else is filled.

---

## Title

```
[Embedding Request] IIT Delhi / CoRE Stack — 2017-2023 — 4 India LULC sites
```

## 1) Your organization

```
IIT Delhi (CoRE Stack land-use/land-cover project)
```

## 2) Intended use

```
Land-use / land-cover (LULC) classification research over India. We build 10 m LULC maps from
per-pixel embeddings and are studying temporal robustness: training classifiers on several years
and testing on held-out years. We currently have Tessera only for 2024, which limits our
multi-year work to Alpha Earth. Additional Tessera years over the ROIs below would let us train
and evaluate Tessera-based classifiers (and our AE+Tessera fusion) across years, including a tree
species split (acacia vs non-acacia) and a mining false-positive study. Tasks: classification and
temporal cross-year evaluation.
```

## 3) Region of Interest (ROI)

Attach **`sites.zip`** (shapefile of the four rectangles below, WGS84). If you would rather use the
bounding-box fields instead, note the form has room for **one** box, so either file one issue per
site with its corners below, or just use the shapefile (recommended — all four in one request).

Per-site bounding-box corners (lon,lat, 4 decimals) — fallback only:

| Site | Left Top | Right Top | Left Bottom | Right Bottom |
|------|----------|-----------|-------------|--------------|
| IIT Delhi + Sanjay Van | 77.1650,28.5600 | 77.2050,28.5600 | 77.1650,28.5200 | 77.2050,28.5200 |
| Asola Bhatti | 77.1900,28.4800 | 77.2700,28.4800 | 77.1900,28.4200 | 77.2700,28.4200 |
| Jalpaiguri | 88.6800,26.5600 | 88.7800,26.5600 | 88.6800,26.4800 | 88.7800,26.4800 |
| Assam tea belt | 95.7500,27.7300 | 95.9800,27.7300 | 95.7500,27.5500 | 95.9800,27.5500 |

## 4) Years (multi-select)

Select: **2019, 2020, 2021, 2022, 2023**  (we already have 2024; add 2017-2018 if you want a longer
back-catalogue.)

## Additional info (optional)

```
Near-year / near-area substitution is acceptable where exact coverage is unavailable. No hard
deadline. Sites are small (a few km each) over India; India currently appears to be 2024-only in
geotessera, which is the gap this request addresses. Happy to provide more detail on the use case.
```

---

*The shapefile was generated by `make_shapefile.py` in this folder; rerun it if you edit the site
boxes.*
