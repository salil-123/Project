1. inference datasets need some kind of a descriptor on what are the features and what is the input data and what it works on;
dataset card will have a type field : training or inference; in training, you dont need embedding just the polygons; 
in inference, its the converse you just need the features or rather inputs;
model card will have two fields one for training dataset and dataset for inference;
2. extent can just be kept as a bounding box for now and later we can keep it evolving
3. the bounding box size is flexible so we need some kind of arbitrary grid size according to the user's interest 
4. lets keep working on the front end as well
5. this week we will finish the model cards and data cards and you can also have a database in which all those are getting stored
6. sir has also sent a tea and non tea classification and has said "Could be good to try, to split trees into tea and non-tea." on the email;
the file is stored on the root with the name "mining_polygons_india.gpkg".